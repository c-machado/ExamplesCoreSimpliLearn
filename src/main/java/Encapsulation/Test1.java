package Encapsulation;

public class Test1 {
    //this value is not affected by the operations done in the TestPar class
    /**
     * encapsulation lets the user declare new instances of the declared variables or methods and use them
     * without affects its original value**/
    public int i = 1;
}
